---
import { cn } from "@/lib/utils" // Hàm tiện ích gom class tiêu chuẩn của shadcn/ui

interface Props {
  class?: string
}

const { class: className } = Astro.props
---

<div class={cn("mx-auto w-full max-w-[960px] px-4 md:px-8", className)}>
  <slot />
</div>
