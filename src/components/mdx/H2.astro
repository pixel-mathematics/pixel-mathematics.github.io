---
import { cn } from "@/lib/utils"
interface Props {
  class: string
}

const { class: className } = Astro.props
---

<h2 class={cn("mt-4 text-2xl font-bold text-primary first:mt-0", className)}>
  <slot />
</h2>
